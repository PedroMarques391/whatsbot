services:
  whatsbot:
    build: .
    container_name: adabot
    restart: always
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHROME_PATH=/usr/bin/google-chrome-stable
    env_file:
      - ./.env